# Dependencias del proyecto tienda_app
# Por ahora el proyecto utiliza la biblioteca estándar de Python (sqlite3).
# Si decides añadir pruebas o librerías externas, agrégalas aquí.

pytest>=7.0.0  # Recomendado para pruebas unitarias